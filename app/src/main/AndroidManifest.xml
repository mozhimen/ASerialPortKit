<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mozhimen.pidk_serialport_test">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SerialPortKit"
        tools:targetApi="31">

        <activity
            android:name="com.mozhimen.pidk_serialport_test.ServiceReceiveActivity"
            android:exported="true">

        </activity>

        <activity
            android:name="com.mozhimen.pidk_serialport_test.SendReceiveActivity"
            android:exported="true">

        </activity>

        <activity
            android:name="com.mozhimen.pidk_serialport_test.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="com.mozhimen.pidk_serialport_test.SerialPortService" />
    </application>

</manifest>